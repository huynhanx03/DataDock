export const APP_ROUTES = Object.freeze({
  home: '/',
  connections: '/connections',
  query: '/query',
  savedQueries: '/saved',
  history: '/history',
  operations: '/operations',
  settings: '/settings',
})
